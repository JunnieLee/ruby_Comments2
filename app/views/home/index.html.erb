<h1> => 이곳은 index 페이지 입니다! </h1>

<!--새로운 게시물의 제목과 내용을 입력-->
<div class="container">
  <form style="margin-top:40px;" action="/home/write" method="POST">
    <div class="form-group">
      <label>Title</label>
      <input type="text" name="title" class="form-control" id="exampleInputEmail1" placeholder="제목을 입력하세요!">
    </div>
      <label>Content</label>
      <textarea class="form-control" rows="5" name="content" placeholder="내용을 입력하세요!"></textarea>
      <br>
      <button type="submit" class="btn btn-default">Submit</button>
  </form>

  <!-- 기존에 존재하는 각각의 게시물들에 대해...-->
  <br>
  <% @posts.each do |p| %>
    <!--게시물의 제목과 내용 노출-->
    <div class="well">
      <span style="font-weight:bold;"><%= p.title %></span> <br>
      <%= p.content %>

    <!--댓글창--> 
    <br><br><br>
      <% p.replies.each do |r| %>
        <p> <%= r.content %> </p>
      <% end %>
    <!--새로운 댓글 입력하기--> 
    <form style="margin-top:40px;" action="/home/reply_write" method="POST">
        <input type="hidden" value="<%=p.id%>" name="post_id">
        <textarea class="form-control" rows="1" name="content" placeholder="댓글을 입력하세요!"></textarea>
        <br>
        <button type="submit" class="btn btn-default">Submit</button>
    </form>

    </div>

  <% end %>

</div>